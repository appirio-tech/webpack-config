machine:
  node:
    version: 5.2.0


compile:
  override:
    - npm install -d -g
    - webpack

deployment:
  development:
    pre: 
        - npm-prepublish --verbose
    override:
        tag: /v[0-9]+(\.[0-9]+)*/
        owner: appirio-tech
        commands:
          - npm publish
